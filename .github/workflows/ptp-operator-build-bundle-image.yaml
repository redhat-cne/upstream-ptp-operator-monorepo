name: "ptp-operator: Deploy Bundle Images to GHCR"
on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  packages: write
  contents: read

jobs:
  build-bundle:
    uses: redhat-cne/upstream-ptp-operator-monorepo/.github/workflows/_reusable-ptp-operator-build-bundle-image.yml@main
    secrets: inherit
